import React from 'react';
const RoomDetail = (props) => {
    const {rooms}= (props);
    return (
           
        { 
        
        
        
        }
   
    );
};

export default RoomDetail;